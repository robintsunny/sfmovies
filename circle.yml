machine:
  node:
    version: 6.11.1

  environment:
    NODE_ENV: 'test'

general:
  artifacts:
    - ~/coverage.tar

dependencies:
  pre:
    - echo -e $npmrc > ~/.npmrc

test:
  post:
    - npm run lint
    - npm run enforce
    - rm -rf $CIRCLE_ARTIFACTS/* && tar cvf ~/coverage.tar coverage
